#include <bits/stdc++.h>
using namespace std;

int main()
{
    int x1, y1, x2, y2;
    cin >> x1 >> y1 >> x2 >> y2;

    double m1 = (x1 + x2) / 2.0;
    double m2 = (y1 + y2) / 2.0;

    cout << m1 << "," << m2 << "\n";

    return 0;
}